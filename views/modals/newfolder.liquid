<div class="modal-header">
    <h5 class="modal-title">Confirmation</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span>&times;</span>
    </button>
</div>
<div class="modal-body">
    Do you want to create a new folder?
    <form class="form-inline mt-2 w-100">
        <div class="form-group w-100">
            <label class="mr-2" for="newNameInput">Folder Name:</label>
            <input type="text" class="form-control flex-fill" id="newNameInput" placeholder="Folder Name"
                value="New Folder">
        </div>
    </form>
</div>
<div class="modal-footer">
    <input type="hidden" id="hidden_file_id" value="{{id}}">
    <button type="button" class="btn btn-primary" data-dismiss="modal"
        onclick="apiPostAjax('id='+document.getElementById('hidden_file_id').value+'&action=newfolder&newname='+document.getElementById('newNameInput').value);">Yes</button>
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
</div>
